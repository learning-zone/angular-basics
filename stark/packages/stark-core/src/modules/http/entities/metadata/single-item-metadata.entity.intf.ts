import { StarkWarnings } from "./metadata-warnings.entity.intf";

/**
 * The StarkSingleItemMetadata interface extends the StarkWarnings interface that describes the "warnings" property with no additions.
 */
export interface StarkSingleItemMetadata extends StarkWarnings {}
